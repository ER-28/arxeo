NODE_ENV=development
PORT=4000

# MongoDB
MONGO_URI=mongodb://arxeo:arxeo_dev_password@localhost:27017/arxeo?authSource=admin

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=arxeo_dev_password

# SMTP (Mailpit)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@arxeo.dev

# MinIO
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=arxeo
MINIO_SECRET_KEY=arxeo_dev_password
MINIO_BUCKET=arxeo
MINIO_USE_SSL=false

# JWT
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Frontend URL
FRONTEND_URL=http://localhost:3001
